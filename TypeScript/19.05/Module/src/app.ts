import {add, multiply} from "./mathUtils"
import {subtract as minus} from './mathUtils'

const sum = add(11,23);
const product = multiply(5,6)
const diference = minus(10,22)
console.log(`Sum: ${sum}`);
console.log(`Product: ${product}`);
console.log(`Difere nce: ${diference}`);
