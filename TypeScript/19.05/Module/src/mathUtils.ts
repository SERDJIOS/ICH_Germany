// Создайте файл `mathUtils.ts`:

// В этом файле определите две простые функции: одну для сложения чисел, другую для их умножения.
// Экспортируйте (именнованно) эти функции, чтобы они были доступны в других файлах.

// Создайте файл `app.ts`:

// В этом файле необходимо использовать функции, которые определили в `mathUtils.ts`.
// Импортируйте их и вызовите с примерами данных, чтобы убедиться, что все работает правильно.

// Запустите проект:

// Чтобы увидеть результаты, скомпилируйте TypeScript в JavaScript (с помощью команды `tsc`, если у вас настроен `tsconfig.json`) и выполните скомпилированный файл `app.js` (например, через Node.js).

export function add(a: number, b: number): number {
    return a + b;
}

export function multiply(a: number, b: number): number{
    return a * b;
}


// Обновите файл `mathUtils.ts`:

// Добавьте функцию `subtract`, которая будет экспортироваться по умолчанию.

// Обновите файл `app.ts`:

// Импортируйте функцию `subtract` из модуля `mathUtils.ts`, но под другим именем (например, `minus`), и выполните вычисления с её использованием.

// Проверьте результат:

// Скомпилируйте и выполните проект, чтобы убедиться, что всё работает корректно и функции выполняют нужные операции.

export function subtract(a: number, b: number) : number{
    return a - b;
}


// Настройте проект:

// Создайте файл `tsconfig.json` в корневой папке вашего проекта.
// Добавьте в него настройки для работы с модулями, такие как `module`, `moduleResolution`, и `outDir`.

// Структурируйте код:

// Переместите ваши файлы `mathUtils.ts` и `app.ts` в папку `src`.
// Убедитесь, что в `tsconfig.json` правильно указана папка для исходных файлов и скомпилированного кода.

// Скомпилируйте проект:

// Выполните команду для компиляции (`tsc` или `npm run build`).
// Проверьте, что скомпилированные файлы находятся в папке `dist`.

// Проверьте работу экспорта и импорта:

// Запустите скомпилированный файл `app.js` (например, командой `node dist/app.js`) и убедитесь, что функции, импортированные из модуля `mathUtils.ts`, работают корректно.

